# 张导请回答设计系统

这是本项目的正式设计基线。后续新增页面、状态和组件时，应先复用这里的语义变量与交互规则，再考虑扩展。

## 1. 项目与设计方向

- **产品类型：** App / Product UI，移动优先的单任务表单工具
- **平台：** 响应式 Web，主要面向手机、微信和抖音内置浏览器
- **目标用户：** 希望向公司直播提交产品问题、反馈、投诉或建议的普通观众
- **主目标：** 第一次打开也能立即开始填写，在最少步骤内完成可信、安全的留言提交
- **视觉气质：** 克制、可信、夜间电台感、成熟科技产品，而不是营销落地页或后台面板
- **设计概念：** “夜航校准单”——页面像一条安静的直播信号通道，细窄青色信号线、等宽编号和连续单列表单共同引导用户从 01 写到 05。
- **对抗审查结论：** 初审遮住品牌名后仍能看出深海军蓝、信号青和 01–05 的校准秩序，但普通裸编号还不够独特。已将编号改为会随字段焦点点亮的信号节点，并把模板感较强的英文 kicker 改成中文语义标签；复审后无需增加装饰或额外步骤。

参考语言来自 [M42 Blog](https://m42-blog.chtne.com/) 的深色层级与冷色高光、[NRadio](https://nradio.fallaxaura.com/) 的夜间电台气质、[GOV.UK question pages](https://design-system.service.gov.uk/patterns/question-pages/) 的直接提问与错误就近原则，以及 [Tally](https://tally.so/) 的低干扰表单和渐进披露。只提取这些设计动作，不复制布局。

文案使用平实、温和、确定的中文。按钮以“提交、查询、发送、关闭”等具体动词开头；加载态说明正在发生什么；错误信息告诉用户如何恢复，不显示系统术语、供应商错误或虚构数据。

## 2. 色彩系统

所有实现使用 `src/styles/tokens.css` 的语义变量。Hex 是当前 OKLCH 在 sRGB 下的近似值，CSS 中仍以 OKLCH 为准。

| 角色 | OKLCH | Hex | 用途 |
|---|---|---|---|
| 页面背景 | `oklch(0.17 0.028 254)` | `#07101b` | 60% 主背景 |
| 深层背景 | `oklch(0.135 0.026 254)` | `#030812` | 输入框、回执编号 |
| 表单表面 | `oklch(0.215 0.034 253)` | `#0e1a29` | 30% 桌面表单、卡片、对话框 |
| 抬升表面 | `oklch(0.285 0.048 248)` | `#152c41` | 导航、悬停、次按钮 |
| 悬停表面 | `oklch(0.33 0.052 246)` | `#1d384f` | 明确悬停反馈 |
| 主文字 | `oklch(0.977 0.008 245)` | `#f3f8fd` | 标题、字段名、关键数据 |
| 次文字 | `oklch(0.82 0.027 247)` | `#b7c6d5` | 正文、说明 |
| 弱化文字 | `oklch(0.74 0.032 249)` | `#9cadbf` | 辅助信息、编号、计数 |
| 信号青 | `oklch(0.84 0.13 218)` | `#4edfff` | 10% 主按钮、链接、焦点 |
| 信号青悬停 | `oklch(0.88 0.115 216)` | `#72ebff` | 主交互悬停 |
| 强调色上文字 | `oklch(0.16 0.025 246)` | `#050e17` | 青色按钮文字 |
| 成功 | `oklch(0.79 0.12 162)` | `#6bd3a3` | 已回复、成功状态 |
| 成功表面 | `oklch(0.28 0.055 162)` | `#073120` | 回复块、成功图标底 |
| 警告 | `oklch(0.85 0.13 83)` | `#f7c662` | 冷却提示 |
| 错误 | `oklch(0.78 0.13 28)` | `#ff9688` | 校验错误、危险状态 |
| 错误表面 | `oklch(0.28 0.07 28)` | `#451914` | 删除悬停等危险反馈 |
| 中性状态 | `oklch(0.68 0.025 248)` | `#8d9aa7` | 未回复状态 |

边框只用于输入控件和上传区的功能边界，使用半透明 `oklch(0.56 0.055 237 / 0.34)`；大面积区域通过背景明度和单层阴影分隔，不堆叠描边。页面只提供深色主题，不做原始 Hex 反转。对比度由 `pnpm run check:contrast` 计算：正文/页面 17.91:1、次文字/页面 10.98:1、弱化文字/表单 7.62:1、主按钮 12.26:1，状态与错误文字也都高于 4.5:1。

## 3. 字体与排版

品牌气质属于“克制的人文科技”。中文优先使用 `HarmonyOS Sans SC`、`MiSans`、`PingFang SC`、`Noto Sans SC`，系统字体兜底；编号与回执使用 `SFMono-Regular`、`Cascadia Code` 或 `Roboto Mono`。不下载远程字体，减少大陆网络依赖和首屏抖动。

| 等级 | 尺寸 |
|---|---|
| Display | `clamp(2rem, 7vw, 3rem)`，行高 1.08 |
| H1 | `clamp(1.875rem, 6vw, 2.5rem)` |
| H2 | `clamp(1.5rem, 4vw, 2rem)` |
| H3 | `1.125rem` |
| Body large | `1.0625rem` |
| Body | `1rem`，行高 1.625 |
| Body small | `0.9375rem` |
| Caption | `0.875rem` |
| Button | `1rem`，字重 760 |

正文不低于 16px，说明文字只在不承担主要阅读任务时使用 14–15px。标题紧凑、正文舒展，中文段落以约 38rem 为最大阅读宽度；移动端标题自然缩小，不截断、不省略。

## 4. 布局与响应式

- 页面壳最大宽度 74rem，主内容列最大宽度 42.5rem，成功页最大宽度 34rem。
- 间距只使用 4、8、12、16、20、24、32、40、48、64px 八档变量。
- 主页面顺序固定为：全局导航 → 简短任务说明 → 连续表单 → 次级历史入口 → 安全提醒页脚。
- 主 CTA 只有“提交留言”；“我的留言”始终可发现，但在层级上保持次要。
- 手机小于 640px 时移除表单外层卡片、阴影和多余内边距，让表单直接成为页面；桌面才使用单一承载表面。
- 手机端以 320、375、390、430px 四档宽度校验，再补充 768px、1280px；同时用 420px 的短可视高度模拟输入法弹起，检查导航、表单和底部弹层。长表单允许自然纵向滚动，不强行做全屏 section。
- 手机壳层同时使用 `viewport-fit=cover` 与四向 `safe-area-inset-*`，正文、页脚和弹层不会贴进刘海或 Home 指示条；支持动态视口单位的浏览器使用 `dvh`，旧 WebView 依次回退到 `svh` 和 `vh`。
- 本产品没有营销 Hero、横向卡片轨道或滚动特效。开场区只回答“这是做什么的”和“需要多久”。
- 移动对话框从底部出现，宽度占满、保留安全区；桌面居中且最大 34rem。标题和底部操作区固定可见，只有正文区域滚动。原生 `dialog` 负责焦点限制和关闭后的焦点恢复。

## 5. 组件规则

组件以语义 HTML、原生表单控件和项目 CSS 为基础，不混用第二套 UI 库。

- **导航：** 文本品牌加三柱信号标记；双入口分段导航，当前项使用信号青实底。手机隐藏导航图标但保留文字，品牌和每个导航入口的触控高度均不低于 44px。
- **主按钮：** 52px 最小高度，青色实底；默认、悬停、键盘焦点、按下、加载、禁用状态齐全。相同意图只使用同一标签。
- **次按钮：** 抬升深色表面；静默按钮只用于关闭、清草稿等低优先动作。
- **表单字段：** 字段编号、名称、必填标记在同一行；字段名称只占自身内容宽度，行内其余空白不触发聚焦或上传。帮助文字在控件前，错误在控件后并由 `aria-describedby` 关联。输入和选择控件最小高 52px。
- **上传：** 一条虚线操作区，选中后最多三张方形缩略图；每张提供可访问名称且不小于 44px 的移除按钮和处理后文件大小。图片在浏览器和 Worker 各压缩一次，不以编码后的字节数拒绝用户，但继续限制格式、边长和总像素数。
- **确认项：** 原生 checkbox，政策名称是就地按钮；不能用预选或仅靠颜色表示错误。
- **OTP：** 主表单之外的底部弹层，六个数字格支持粘贴、短信自动填充、方向键和退格移动；冷却显示服务端时间换算后的秒数。安卓输入法按 `interactive-widget=resizes-content` 收缩可视区域，短屏状态压缩弹层留白并保持确认按钮可见。
- **历史卡片：** 每条留言只保留一个层级；状态、时间、主题、正文、图片数量、回复依次展示。公开状态只有“已回复/未回复”。
- **空/加载/错误：** 请求按钮显示具体进行态；启动失败给“重新连接”；查询未命中显示统一文字；列表没有额外装饰性空状态插画。
- **微信兼容：** 生产构建以 Chrome 66、Safari 12 为 JavaScript 下限，并按 Vite 的微信 WebView 建议将 CSS 下限设为 Chrome 61；入口先补齐依赖库所需的 `globalThis` 与 `Object.fromEntries`。浏览器侧 UUID 在 `randomUUID` 不可用时继续使用 `getRandomValues` 安全生成；HTML 预置可读的启动提示，React 渲染异常也必须显示恢复操作，不能只留下背景空白页。

圆角使用 12px 控件、14px 按钮、16px 分组、24px 页面表面四档。只有页面表面、对话框和历史卡片使用克制阴影；禁止嵌套卡片、玻璃拟态和渐变文字。唯一渐变是页面顶部低透明度的青色径向环境光，不承载信息。

## 6. 图标与图片

图标统一使用 Phosphor Icons：导航 16px、按钮约 19px、独立状态 32–36px。只在图标能帮助理解动作或状态时使用；纯图标按钮必须有 `aria-label`。

品牌标记由 CSS 绘制三柱信号，不依赖位图 Logo。用户图片保持自然裁切，缩略图 `object-fit: cover`；浏览器与 Worker 两次处理后统一为 WebP。页面不使用库存图、AI 插画或装饰照片，避免分散填写注意力。

## 7. 动效与反馈

交互只使用 CSS，标准过渡 220ms、快速过渡 160ms，缓动为 `cubic-bezier(0.2, 0.7, 0.2, 1)`。悬停改变表面或文字，按下轻微缩放；新增“其他主题”字段以短距离淡入。加载 spinner 只旋转，启动信号只做短脉冲。

尊重 `prefers-reduced-motion`，届时动画和过渡缩短至 1ms。所有可点击元素使用指针光标，禁用项使用禁用光标。异步结果由按钮状态、就地错误或成功回执表达，不用只靠颜色，也不使用自动消失 Toast。

性能目标为 LCP < 2.5s、CLS < 0.1、INP < 200ms。系统字体避免阻塞；历史页和成功页按路由拆包；图片压缩优先在 Web Worker 执行；静态 hashed 资源长期缓存，API 明确 `no-store`。

## 8. 可访问性

- 页面包含跳到主要内容链接、header/main/footer landmark，Tab 顺序与视觉顺序一致。
- 所有交互控件都有清晰的 `:focus-visible` 三像素焦点环；高对比模式为关键表面补充实体边界。
- 错误通过文字、`aria-invalid`、`aria-describedby` 与 live region 同时表达。
- 原生 `dialog` 提供模态语义；关闭按钮有名称，打开后焦点进入弹窗，关闭后回到触发控件。
- 表单可由键盘完整操作，OTP 支持粘贴和 `one-time-code`，手机输入声明正确的 `inputMode` 与 `autocomplete`。
- 支持系统文字放大和 200% 缩放，不固定正文区域高度；所有布局使用逻辑方向属性，未来可在不重排结构的情况下支持 RTL。

## 9. 项目专属反模板规则

只允许本文件列出的深海军蓝、冷灰、信号青与语义状态色；不用纯黑或纯白。遮住品牌名后，页面仍应通过三柱信号、短青线、01–05 等宽编号和连续校准式表单被识别。任何新页面如果退化成“深色大圆角卡片 + 任意图标 + 大段营销标题”的通用 SaaS 模板，即视为不合格。

用户需求明确要求长表单自然滚动、避免大型营销 Hero，因此本 App/Product 页面不采用全屏 section、横向轮播或装饰性图像；这些是经过需求确认的项目例外，不应在后续被“补齐”。

## 10. Studio 桌面工作台扩展

Studio 延续“夜航校准单”，但把表单侧的信号通道转化成内部“夜航调度台”：固定侧栏表达处理层级，等宽编号和时间用于定位，青色只标记当前路由、主要操作和直播信号，内容卡片保持紧凑、安静且可连续阅读。

- **布局：** 1024px 以上使用 17rem 固定侧栏与独立内容区，列表内容最大 96rem；更窄视口改为顶部菜单。统计是带分隔线的单一 `<dl>`，不做仪表盘式四卡片、图表或趋势。
- **导航：** 侧栏严格保持“留言 → 未回复 / 已回复 → 全部、直播、留言 / 待办”的信息层级，其中“已回复”使用原生 `details` 折叠分组，支持鼠标和键盘操作；底部放直播展示模式和当前账号退出。公开端不出现 Studio 入口。
- **列表：** 桌面宽屏两列、普通桌面与移动端单列；未回复及已回复列表在总数左侧提供留言主题筛选，筛选条件写入 URL，并由服务端同时作用于总数、分页和新留言提示。状态、编号、昵称、主题、两行正文、图片数、时间和回复摘要共用一个卡片层级。待办按钮只出现在未回复概览卡片底部。
- **详情：** 内容顺序固定为昵称、主题、完整正文、提交时间、图片、手机号、历史回复、回复表单。历史回复使用细窄成功色时间线，不再嵌套独立卡片；底部左返回、右提交。
- **直播模式：** 隐藏侧栏、搜索和统计，放大正文与卡片内边距，顶部只保留全屏与退出操作；不显示待办操作或完整手机号，回复类型锁定为直播回复。
- **媒体与模态：** 图片缩略图使用自然裁切，原生全屏 dialog 负责放大、前后切换、关闭和下载。确认 dialog 只用于普通回复与退出登录；直播回复直接提交。
- **响应式与可访问性：** 必查 375×700、768×800、1280×800；不允许横向溢出、底部操作被裁切或焦点不可见。所有图标来自 Phosphor，纯图标按钮必须有名称，键盘可完成导航、回复、图片切换和退出。

## 11. 后续更新规则

未来页面必须复用同一色彩、字阶、间距、圆角、按钮、表单、图标和状态语言。若这些基础发生变化，应在同一次改动中更新本文件和 `tokens.css`，并重新运行对比度、三档视口、键盘焦点和横向溢出检查，避免设计文档与实际界面漂移。
